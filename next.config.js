/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
      domains: ['agency.teamrabbil.com', 'mainfacts.com', 'upload.wikimedia.org']
    },
    poweredByHeader: false,
  }
